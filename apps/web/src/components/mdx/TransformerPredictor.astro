---
// Author-facing wrapper: lets MDX use <TransformerPredictor /> with no
// imports while keeping the hydration directive out of content files.
import { TransformerPredictor as Widget } from "@opends/widgets";
---

<Widget client:visible {...Astro.props} />
